name: app_taller1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.1

dependencies:
  chewie: ^1.14.1
  flutter:
    sdk: flutter
  http: ^1.6.0
  image_picker: ^1.2.3
  supabase_flutter: ^2.15.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/json/peliculas.json
     